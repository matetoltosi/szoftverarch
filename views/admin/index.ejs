<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Admin Panel</title>
</head>
<body>
  <h1>Admin Panel</h1>
  <p><a href="/">Back to home</a></p>
  <h2>Users</h2>
  <ul>
    <% users.forEach(u => { %>
      <li><%= u.username %> — <%= u.role %> — <%= u.isActive ? 'active' : 'inactive' %></li>
    <% }) %>
  </ul>
</body>
</html>