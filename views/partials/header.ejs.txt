<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tanulmányi Rendszer</title>
</head>
<body>
    <nav>
        <ul>
            <% if (!user) { %>
                <li><a href="/">Bejelentkezés</a></li>
            <% } else { %>
                <li>Bejelentkezve: <strong><%= user.name %></strong> (<%= user.role %>)</li>
                
                <% if (user.role === 'ADMIN') { %>
                    <li><a href="/admin/dashboard">Vezérlőpult</a></li>
                    <li><a href="/admin/users">Felhasználók kezelése</a></li>
                    <li><a href="/admin/subjects">Tantárgyak</a></li>
                <% } %>
                
                <% if (user.role === 'STUDENT') { %>
                    <li><a href="/student/dashboard">Órarend / Tárgyak</a></li>
                    <li><a href="/student/exams">Vizsgajelentkezés</a></li>
                    <li><a href="/student/finances">Pénzügyek</a></li>
                <% } %>

                <% if (user.role === 'TEACHER') { %>
                    <li><a href="/teacher/dashboard">Kurzusaim</a></li>
                    <li><a href="/teacher/grading">Osztályozás</a></li>
                <% } %>

                <li><a href="/auth/logout">Kijelentkezés</a></li>
            <% } %>
        </ul>
        <hr>
    </nav>
    <main>
        <% if (error && error.length > 0) { %>
            <div style="color: red; border: 1px solid red; padding: 10px;">
                <%= error %>
            </div>
        <% } %>
        <% if (success && success.length > 0) { %>
            <div style="color: green; border: 1px solid green; padding: 10px;">
                <%= success %>
            </div>
        <% } %>